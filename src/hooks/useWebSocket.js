// WebSocket hook removed — all data is fetched via polling the Node-RED REST API.
// This file is kept as a placeholder in case Server-Sent Events or WebSocket
// support is added to Node-RED in the future.

export function useWebSocket() {
  // No-op: real-time updates come from React Query polling against Node-RED
}
